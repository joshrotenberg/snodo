# Frozen 2026-07-28 conformance run

Run date: 2026-08-25  
Runner: `@modelcontextprotocol/conformance@0.2.0-alpha.11`  
Requirements: frozen `2026-07-28` server inventory (37 scenarios)

## Honest score

The exercised whole-scenario score is **22/37**. All 37 required scenarios were
attempted. A scenario counts here only when its fixture was present, its intended
behavior was exercised, and the runner reported no `FAILURE` checks.

The passing scenarios are:

- `completion-complete`
- `tools-list`
- `tools-call-simple-text`
- `tools-call-image`
- `tools-call-audio`
- `tools-call-embedded-resource`
- `tools-call-mixed-content`
- `tools-call-error`
- `server-sse-multiple-streams`
- `resources-list`
- `resources-read-text`
- `resources-read-binary`
- `resources-templates-read`
- `sep-2164-resource-not-found`
- `prompts-list`
- `prompts-get-simple`
- `prompts-get-with-args`
- `prompts-get-embedded-resource`
- `prompts-get-with-image`
- `dns-rebinding-protection`
- `caching`
- `input-required-result-unsupported-methods`

Across the scored requirements, the runner reported 89 `SUCCESS`, 15 `FAILURE`,
5 `SKIPPED`, 2 `WARNING`, and 1 `INFO` checks.

## Raw runner result and exclusions

The raw runner had no `FAILURE` check in 25/37 required scenarios. Three of those
are deliberately excluded from the 22/37 exercised score:

| Scenario | Why it is excluded |
|---|---|
| `input-required-result-missing-input-response` | Its required fixture was absent; the runner emitted a warning with no failure check. |
| `input-required-result-ignore-extra-params` | Its required fixture was absent; the runner emitted a warning with no failure check. |
| `input-required-result-validate-input` | Its required fixture was absent; method-not-found produced a false-positive no-failure result. |

This distinction prevents missing fixtures and warning-only paths from becoming
claimed protocol support.

## Pending, not-scored scenarios

Two pending scenarios passed every check but are not part of the frozen
37-scenario score:

- `json-schema-2020-12`: 8/8 checks passed.
- `http-header-validation`: 14/14 checks passed.

One other pending scenario was measured but did not pass:

- `http-custom-header-server-validation`: 1/6 checks passed; the fixture exposes
  no tool with `x-mcp-header` annotations, so five checks were not testable and
  reported as failures.

The machine-readable companion is
[`2026-07-28-alpha.11-summary.json`](2026-07-28-alpha.11-summary.json).
