defmodule Snodo do
  @moduledoc """
  A router-first architecture spike for the Model Context Protocol.

  The core router and component APIs are ordinary immutable data. Processes are
  introduced only at transport and supervision boundaries.
  """
end
